enum PickUpState { beforePickUp, waitingPickUp, afterPickUp }

enum TripState { ongoing, finish, userCancel, driverCancel }

enum SoSType { user, driver }

enum PaymentType { cash, pay }

enum RentType { individual, cooperate }

enum PlaceType { recent, search, savePlace }

enum PromotionType { mmk, percentage }

enum SosUserType { user, driver }

enum SavePlaceType { Home, Work, Other }

enum RentPayType { account, credit }
